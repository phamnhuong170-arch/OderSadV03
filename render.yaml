services:
  - type: web
    name: gamek
    runtime: node
    buildCommand: echo "no build needed"
    startCommand: node --experimental-sqlite server.js
    envVars:
      - key: NODE_ENV
        value: production

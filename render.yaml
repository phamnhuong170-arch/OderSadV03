services:
  - type: web
    name: techvn-backend
    runtime: node
    buildCommand: npm install
    startCommand: node --experimental-sqlite src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: "*"
